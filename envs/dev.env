# CLI environment variables for local development
# These configure where the CLI connects to

# API endpoint for Stories/Components remote client
API_BASE_URL=http://localhost:4000

# OAuth server URL for authentication flows
OAUTH_BASE_URL=https://special-mutually-falcon.ngrok-free.app
