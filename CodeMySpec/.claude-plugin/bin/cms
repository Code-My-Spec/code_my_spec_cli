#!/bin/bash
# CodeMySpec CLI wrapper
# In dev: runs via mix
# In prod: replaced with compiled binary

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
CLI_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"

cd "$CLI_ROOT"
exec mix run --no-halt
