{
  "_comment": "STDIO transport fallback - copy to plugin.json if HTTP server isn't running",
  "name": "codemyspec",
  "description": "Generate context specifications for your codebase",
  "version": "1.1.0",
  "author": {
    "name": "CodeMySpec"
  },
  "mcpServers": {
    "architecture-server": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/mcp",
      "args": []
    }
  }
}
